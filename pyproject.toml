[project]
name = "lm-arena-time-analysis"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "kaleido>=1.3.0",
    "pandas>=3.0.4",
    "plotly>=6.8.0",
    "pyarrow>=24.0.0",
]
